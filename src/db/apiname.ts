const baseurls = "http://localhost:3000/api/profissional";
// const baseurls = 'https://chapa100.onrender.com/api/restaurante/admin';

export default class routersName {
    questions = `${baseurls}/questions`;
  exame = `${baseurls}/exame`;
  user = `${baseurls}/user`;
  
}
